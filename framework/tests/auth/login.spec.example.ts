/**
 * EXAMPLE ONLY — not executed by Playwright (`.example.ts` suffix).
 * Copy to `login.spec.ts` when implementing auth tests.
 *
 * @see docs/test-organization.md
 * @see .ai/templates/test.template.ts
 */
// import { test, expect } from '@fixtures/base.fixture';
// import { standardUser } from '@data/users';
// import { LoginFlow } from '@flows/login.flow';
//
// test('@smoke @regression user can log in', async ({ page, loginPage }) => {
//   const loginFlow = new LoginFlow(loginPage);
//
//   await loginFlow.loginAs(standardUser);
//
//   await expect(page).toHaveURL(/inventory/);
// });

export {};
